// ? Because of the importance of connecting actions and reducers regarding the type variables, we can create a separate file with just those variables, so we can use them in both action.js and reducer.js

export const ADD_TO_CART="ADD_TO_CART"